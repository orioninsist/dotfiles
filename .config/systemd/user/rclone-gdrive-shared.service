[Unit]
Description=Read-only Google Drive shared-with-me mount
Documentation=https://rclone.org/drive/
After=network-online.target
Wants=network-online.target
ConditionPathExists=%h/.config/rclone/rclone.conf

[Service]
Type=notify
ExecStartPre=/usr/bin/mkdir -p %h/GoogleDrive-Shared
ExecStart=/usr/bin/rclone mount gdrive: %h/GoogleDrive-Shared \
  --config %h/.config/rclone/rclone.conf \
  --drive-shared-with-me \
  --read-only \
  --vfs-cache-mode off \
  --dir-cache-time 5m \
  --poll-interval 1m
ExecStop=/usr/bin/fusermount3 -uz %h/GoogleDrive-Shared
Restart=on-failure
RestartSec=10

[Install]
WantedBy=default.target
