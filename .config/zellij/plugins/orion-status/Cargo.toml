[package]
name = "orion-status"
version = "0.1.0"
edition = "2021"

[dependencies]
zellij-tile = "0.45.0"
chrono = { version = "0.4", default-features = false, features = ["clock"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
