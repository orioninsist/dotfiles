# Sway native window management
# Contains only compositor-level window and container management.

### Focus movement
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Native focus traversal
bindsym $mod+Alt+n focus next sibling
bindsym $mod+Alt+p focus prev sibling
bindsym $mod+Ctrl+Tab focus next
bindsym $mod+Ctrl+Shift+Tab focus prev

### Container movement
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

### Layout
bindsym $mod+b splith
bindsym $mod+v splitv
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

### Fullscreen
bindsym $mod+f fullscreen

### Floating
floating_modifier $mod normal
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle

### Container hierarchy
bindsym $mod+a focus parent
bindsym $mod+Ctrl+j focus child

### Scratchpad
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# Scratchpad - numeric keypad
bindsym $mod+Shift+KP_Subtract move scratchpad
bindsym $mod+KP_Subtract scratchpad show

### Resize mode
mode "resize" {
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

### Workspace output movement
bindsym $mod+Ctrl+Right move workspace to output right
bindsym $mod+Ctrl+Left move workspace to output left

### Floating window positioning
bindsym $mod+Ctrl+c move position center
bindsym $mod+Ctrl+Up move up 20 px
bindsym $mod+Ctrl+Down move down 20 px
bindsym $mod+Ctrl+Shift+Left move left 20 px
bindsym $mod+Ctrl+Shift+Right move right 20 px

### Container border
bindsym $mod+Ctrl+b border toggle

### Quick mark
bindsym $mod+Ctrl+F7 mark --toggle quick
bindsym $mod+Ctrl+F8 [con_mark="quick"] focus
bindsym $mod+Ctrl+F9 swap container with mark quick

### Output focus
bindsym $mod+Ctrl+Shift+h focus output left
bindsym $mod+Ctrl+Shift+j focus output down
bindsym $mod+Ctrl+Shift+k focus output up
bindsym $mod+Ctrl+Shift+l focus output right

### Container output movement
bindsym $mod+Alt+h move container to output left
bindsym $mod+Alt+j move container to output down
bindsym $mod+Alt+k move container to output up
bindsym $mod+Alt+l move container to output right

### Window marks
set $mark_mode "Mark window [1-9]"
mode $mark_mode {
    bindsym 1 mark --replace mark1; mode "default"
    bindsym 2 mark --replace mark2; mode "default"
    bindsym 3 mark --replace mark3; mode "default"
    bindsym 4 mark --replace mark4; mode "default"
    bindsym 5 mark --replace mark5; mode "default"
    bindsym 6 mark --replace mark6; mode "default"
    bindsym 7 mark --replace mark7; mode "default"
    bindsym 8 mark --replace mark8; mode "default"
    bindsym 9 mark --replace mark9; mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Ctrl+Shift+F7 mode $mark_mode

### Focus marked windows
set $mark_focus_mode "Focus mark [1-9]"
mode $mark_focus_mode {
    bindsym 1 [con_mark="mark1"] focus; mode "default"
    bindsym 2 [con_mark="mark2"] focus; mode "default"
    bindsym 3 [con_mark="mark3"] focus; mode "default"
    bindsym 4 [con_mark="mark4"] focus; mode "default"
    bindsym 5 [con_mark="mark5"] focus; mode "default"
    bindsym 6 [con_mark="mark6"] focus; mode "default"
    bindsym 7 [con_mark="mark7"] focus; mode "default"
    bindsym 8 [con_mark="mark8"] focus; mode "default"
    bindsym 9 [con_mark="mark9"] focus; mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Ctrl+Shift+F8 mode $mark_focus_mode

### Complete native Sway window-management mode
# Exposes useful official sway(5) window/container commands without scripts.
set $window_mode "Window management"
mode $window_mode {
    # Focus layers and hierarchy
    bindsym h focus left
    bindsym j focus down
    bindsym k focus up
    bindsym l focus right
    bindsym n focus next
    bindsym p focus prev
    bindsym bracketright focus next sibling
    bindsym bracketleft focus prev sibling
    bindsym a focus parent
    bindsym c focus child
    bindsym t focus tiling
    bindsym f focus floating
    bindsym m focus mode_toggle

    # Split and layout
    bindsym b splith
    bindsym v splitv
    bindsym x split toggle
    bindsym BackSpace split none
    bindsym s layout stacking
    bindsym w layout tabbed
    bindsym e layout toggle split
    bindsym d layout default
    bindsym q layout toggle all

    # Window state
    bindsym F1 fullscreen toggle
    bindsym F2 fullscreen toggle global
    bindsym F3 floating toggle
    bindsym F4 floating enable
    bindsym F5 floating disable
    bindsym F6 sticky toggle
    bindsym Delete kill

    # Move / destination
    bindsym Left move left
    bindsym Down move down
    bindsym Up move up
    bindsym Right move right
    bindsym z move position center
    bindsym u move position cursor
    bindsym g move container to mark quick
    bindsym comma move container to workspace prev
    bindsym period move container to workspace next
    bindsym slash move container to workspace back_and_forth

    # Scratchpad / marks / swap
    bindsym minus move scratchpad
    bindsym equal scratchpad show
    bindsym r mark --toggle quick
    bindsym o [con_mark="quick"] focus
    bindsym i swap container with mark quick
    bindsym Delete unmark

    # Border and opacity
    bindsym 1 border none
    bindsym 2 border pixel 3
    bindsym 3 border normal 3
    bindsym 4 border toggle
    bindsym 9 opacity minus 0.05
    bindsym 0 opacity plus 0.05

    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Ctrl+w mode $window_mode

### Window borders
default_border pixel 3
default_floating_border pixel 3

client.focused          #89b4fa #1e1e2e #cdd6f4 #89b4fa #89b4fa
client.focused_inactive #313244 #1e1e2e #a6adc8 #313244 #313244
client.unfocused        #181825 #181825 #6c7086 #181825 #181825
client.urgent           #f38ba8 #1e1e2e #cdd6f4 #f38ba8 #f38ba8
