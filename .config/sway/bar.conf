# Waybar

exec_always --no-startup-id sh -c "pkill -x waybar 2>/dev/null || true; exec waybar -c ~/.config/waybar/config.jsonc -s ~/.config/waybar/style.css"

# Toggle Waybar visibility without stopping the process
bindsym $mod+c exec ~/.config/sway/scripts/waybar-visibility
