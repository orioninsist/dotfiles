#!/usr/bin/env bash
set -euo pipefail

export QT_QPA_PLATFORM="${QT_QPA_PLATFORM:-wayland;xcb}"
export KDE_COLOR_SCHEME=CatppuccinMochaMauve

unset CALIBRE_USE_SYSTEM_THEME QT_QPA_PLATFORMTHEME QT_STYLE_OVERRIDE
exec /usr/bin/calibre "$@"
