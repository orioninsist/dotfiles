#!/usr/bin/env bash
set -euo pipefail

repo="/mnt/local/projects/knowledge"
binary="$repo/src-tauri/target/debug/knowledge-productivity"

if [[ ! -x "$binary" ]]; then
    cd "$repo"
    cargo build --manifest-path src-tauri/Cargo.toml --no-default-features
fi

exec "$binary"
